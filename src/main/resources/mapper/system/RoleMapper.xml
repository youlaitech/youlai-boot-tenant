<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.youlai.boot.system.mapper.RoleMapper">

    <!-- 获取最大范围的数据权限 -->
    <select id="getMaximumDataScope" resultType="java.lang.Integer">
        SELECT
            min(data_scope)
        FROM
            sys_role
        <where>
            <choose>
                <when test="roles!=null and roles.size>0">
                    AND code IN
                    <foreach collection="roles" item="role" separator="," open="(" close=")">
                        #{role}
                    </foreach>
                </when>
                <otherwise>
                    id = -1
                </otherwise>
            </choose>
        </where>
    </select>

    <!-- 获取角色的数据权限列表 -->
    <select id="getRoleDataScopes" resultType="com.youlai.boot.security.model.RoleDataScope">
        SELECT
            code AS roleCode,
            data_scope AS dataScope
        FROM
            sys_role
        <where>
            <choose>
                <when test="roleCodes!=null and roleCodes.size>0">
                    AND code IN
                    <foreach collection="roleCodes" item="roleCode" separator="," open="(" close=")">
                        #{roleCode}
                    </foreach>
                </when>
                <otherwise>
                    id = -1
                </otherwise>
            </choose>
            AND is_deleted = 0
        </where>
    </select>
</mapper>
